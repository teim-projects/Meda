from django.apps import AppConfig

class MedaIntegrationConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'meda_integration'
    verbose_name = 'MEDA Integration'
